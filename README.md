# COGS 127 Handoff Prototype

Static prototype for the COGS 127 handoff/task flow concept.

## Run Locally

```bash
python3 -m http.server 3000
```

Then open `http://localhost:3000`.

## Deploy

This project is ready for Vercel as a static site. There is no build step.
